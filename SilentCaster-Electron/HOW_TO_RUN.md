# Как правильно запустить приложение

## ⚠️ ВАЖНО: Приложение должно запускаться через Electron, а не в браузере!

Если вы видите ошибку "Electron API not available", это означает, что приложение открыто в браузере (http://localhost:4200), а не в Electron.

## ✅ Правильный способ запуска

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
cd SilentCaster-Electron
npm run electron:dev
```

Этот скрипт автоматически:
- Запустит Angular dev server
- Дождется его готовности
- Запустит Electron

### Вариант 2: Ручной запуск

**Терминал 1:**
```bash
cd SilentCaster-Electron
npm start
```

Дождитесь сообщения:
```
** Angular Live Development Server is listening on localhost:4200 **
```

**Терминал 2:**
```bash
cd SilentCaster-Electron
npm run electron
```

## ❌ НЕПРАВИЛЬНО

❌ НЕ открывайте http://localhost:4200 в браузере
❌ НЕ используйте только `npm start` без Electron

## 🔍 Как проверить, что все работает

1. **Откройте DevTools в Electron** (F12 или через меню)
2. **Проверьте консоль** - должны быть сообщения:
   - `[Preload] Preload script loaded`
   - `[Preload] electronAPI exposed successfully`
   - `✅ Electron API is available`
3. **Проверьте User Agent** - должен содержать "Electron"

## 🐛 Диагностика проблем

### Проблема: "Electron API not available"

**Решение:**
1. Убедитесь, что вы запустили `npm run electron` или `npm run electron:dev`
2. НЕ открывайте приложение в браузере
3. Проверьте консоль Electron (DevTools) на наличие ошибок

### Проблема: Preload script не загружается

**Решение:**
1. Проверьте, что `preload.js` существует в корне проекта
2. Проверьте консоль main process (терминал, где запущен Electron)
3. Должно быть сообщение: `Preload script exists: true`

### Проблема: "Illegal return statement"

**Причина:** Код выполняется в браузере, а не в Electron

**Решение:** Запустите приложение через Electron, а не открывайте в браузере

## 📝 Проверка окружения

В консоли DevTools выполните:
```javascript
console.log('Is Electron:', navigator.userAgent.includes('Electron'));
console.log('electronAPI:', window.electronAPI);
```

Если `Is Electron: false` - вы в браузере, нужно запустить через Electron.
Если `electronAPI: undefined` - preload script не загрузился.

