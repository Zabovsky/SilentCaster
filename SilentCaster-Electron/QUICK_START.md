# Быстрый старт SilentCaster Electron

## 🚀 Первый запуск

### 1. Установка зависимостей

```bash
cd SilentCaster-Electron
npm install
```

### 2. Запуск в режиме разработки

**Вариант 1: Автоматический запуск (рекомендуется)**
```bash
npm run electron:dev
```
Этот скрипт автоматически:
- Запустит Angular dev server на http://localhost:4200
- Дождется его готовности
- Запустит Electron приложение

**Вариант 2: Ручной запуск**
```bash
# Терминал 1: Запуск Angular dev server
npm start

# Терминал 2: Запуск Electron (после того как Angular запустится)
npm run electron
```

### 3. Сборка для продакшена

```bash
# Сборка Angular приложения
npm run build:prod

# Запуск Electron с собранным приложением
npm run electron
```

## 🔧 Решение проблем

### Белый экран

Если видите белый экран:

1. **Проверьте, запущен ли Angular dev server:**
   ```bash
   npm start
   ```
   Должен быть доступен на http://localhost:4200

2. **Проверьте консоль Electron:**
   - Откройте DevTools (F12 или через меню)
   - Проверьте ошибки в консоли

3. **Соберите приложение и запустите:**
   ```bash
   npm run build:prod
   npm run electron
   ```

### Ошибки загрузки

Если Electron не может загрузить приложение:

1. Убедитесь, что все зависимости установлены:
   ```bash
   npm install
   ```

2. Проверьте, что Angular CLI установлен глобально (опционально):
   ```bash
   npm install -g @angular/cli
   ```

3. Очистите кэш и переустановите:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

## 📝 Полезные команды

- `npm start` - Запуск Angular dev server
- `npm run build` - Сборка Angular приложения (development)
- `npm run build:prod` - Сборка для продакшена
- `npm run electron` - Запуск Electron (требует собранное приложение)
- `npm run electron:dev` - Запуск в режиме разработки
- `npm run electron:build` - Сборка Electron приложения для распространения

## 🐛 Отладка

1. **Консоль Angular:** Откройте браузер на http://localhost:4200 и используйте DevTools
2. **Консоль Electron:** DevTools открываются автоматически в dev режиме (F12)
3. **Логи Electron:** Проверьте терминал, где запущен Electron

